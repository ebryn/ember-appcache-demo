import { arrayJoin } from 'ember-appcache-demo/helpers/array-join';
import { module, test } from 'qunit';

module('Unit | Helper | array join');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayJoin([42]);
  assert.ok(result);
});
